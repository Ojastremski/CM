<!--
	Olek Jastremski
	14.04.2020
	UWEB - USE WEB EQUIPMENT MANAGEMENT (v 1.0)
	For USEWEB
-->
<!DOCTYPE html>
<html>

   <head>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name = "keywords" content = "HTML, Meta Tags, Metadata" />
        <meta name = "description" content = "Construction management" />
        <meta name = "author" content = "Olek Jastremski" />
        <meta http-equiv = "Content-Type" content = "text/html; charset = UTF-8" />

        <title>CM - Construction Management</title>
        
        {% stylesheets '@AccessBundle/Resources/public/css/bootstrap/*' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}

        {% stylesheets '@AccessBundle/Resources/public/css/fontawesome/*' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}

        {% stylesheets '@AccessBundle/Resources/public/css/app/*' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}

        {% stylesheets '@ConstructionBundle/Resources/public/css/bootstrap-datetimepicker.css' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}

        {% javascripts '@AccessBundle/Resources/public/js/bootstrap/*' %}
            <script src="{{ asset_url }}"></script>
        {% endjavascripts %}

        {% javascripts '@AccessBundle/Resources/public/js/ConstructionList/*' %}
            <script src="{{ asset_url }}"></script>
        {% endjavascripts %}

        {% javascripts '@AccessBundle/Resources/public/js/DateTimePicker/*' %}
            <script src="{{ asset_url }}"></script>
        {% endjavascripts %}

        {% block header %}
        {% endblock %}
   </head>

   <body>

        <header>
            <!-- menu render -->
            {% if is_granted('ROLE_USER') %}
                {{ include('menu.html.twig') }}
            {% endif %}
        </header>

        {% block body %}
	    {% endblock %}

        <footer id="footer">

        </footer>
    </body>

</html>